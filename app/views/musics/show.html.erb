<%= image_tag @music.image.url(:medium) %>
<br>
<%= @music.title %>
<br>
<%= @music.content %>
<br>
<%= @music.theme %>

<% if current_user == @music.user %>
    <%= link_to 'edit', edit_music_path %>
    <%= link_to 'delete', music_path, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>